gateway:
  app-version: SNAPSHOT
  config-path: "src/test/resources/example.gateway.yaml"
  system:
    network-adapter: enp0s25
    platform: SIMULATED
    platform-config:
      someConfigurtion: simulated-test
    mqttHostname: "localhost"
  websocket:
    enabled: true
