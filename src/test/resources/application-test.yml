gateway:
  app-version: TEST
  config-path: "src/test/resources/example-1.0-simulated.gateway.yaml"
  system:
    network-adapter: enp0s25
    platform: SIMULATED
    expander:
      enabled: true

homeassistant:
  enabled: true
  root-topic: "homeassistant"
