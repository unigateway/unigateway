gateway:
  app-version: TEST
  config-path: "src/test/resources/example.gateway.yaml"
  system:
    network-adapter: enp0s25
    platform: SIMULATED
    expander:
      enabled: true
    components:
      serial:
        enabled: true
        device: "/dev/tty1"

homeassistant:
  enabled: true
  root-topic: "homeassistant"