gateway:
  app-version: TEST
  config-path: "src/test/resources/example.gateway.yaml"
  system:
    network-adapter: enp0s25
    platform: SIMULATED
    expander:
      enabled: true
    components:
      mysensors:
        enabled: true
        serial-device: "build/tmp/test-serial"

homeassistant:
  enabled: true
  root-topic: "homeassistant"